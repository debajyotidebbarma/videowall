//HEADER
header.header 
    nav.nav.nav--tours 
        a.nav__el(href='/') VideoWall
    .header__logo 
        img(src='/img/videowall.png' alt='videowall logo')    
    nav.nav.nav--user 
        if user
            a.nav__el(href='/my-videos')
                span=user.name.split(' ')[0]
            a.nav__el.nav__el--cta(href='/logout') Log out
            a.nav__el.nav__el--upload(href='/upload-file') Upload    

        else
            a.nav__el(href='/login') Log in to upload
            a.nav__el.nav__el--cta(href='/signUp') Sign Up